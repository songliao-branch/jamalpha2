#Private repository for TwistJam app @2015.
The app uses cocoapods, make sure every time you `git pull` from master, you do `pod install` in the app directory
Make sure you open `JamAlpha2.xcworkspace`!
